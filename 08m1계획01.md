

# Python & Git 기초 미션 실행 계획서

본 계획서는 수정된 초기 프롬프트 데이터와 제출 요구사항에 맞춰 **환경 설정 $\rightarrow$ Git 저장소 준비 $\rightarrow$ 소스코드 개발 $\rightarrow$ Git 이력 및 브랜치 관리 $\rightarrow$ 결과물 제출**의 5단계 순서로 수행됩니다.

---

## 1단계: 개발 환경 설정 및 검증

1. **VSCode 확장 프로그램 설치**

* `Python` 확장(Extension) 및 `Korean Language Pack`(선택) 설치.




2. **Python 환경 검증**

* 터미널에서 버전을 확인하여 **Python 3.10 이상**인지 확인.


```bash
python --version  # 또는 python3 --version

```


* `hello.py` 파일 작성 후 `print("Hello")` 실행 확인.




3. **Git 사용자 정보 및 기본 설정**

* Git 버전 확인 후 계정 정보 및 기본 브랜치명을 `main`으로 설정.


```bash
git --version
git config --global user.name "사용자 이름"
git config --global user.email "사용자 이메일"
git config --global init.defaultBranch main

```




4. **GitHub 계정 연동**

* VSCode에서 GitHub 로그인 및 연동 상태 점검.





---

## 2단계: Git 저장소 초기화 및 구조 잡기

1. **GitHub 및 로컬 저장소 생성**

* GitHub 새 원격 저장소 생성 후 로컬 폴더에서 저장소 초기화:


```bash
git init

```




2. **초기 파일 생성**

* `.gitignore` 파일 작성 (`__pycache__/`, `.venv/` 등 파이썬 임시파일 제외).


* `README.md` 파일 생성 및 프로젝트 제목 작성.




3. **첫 커밋 및 푸시 (커밋 #1)**

* 원격 저장소 연결 후 커밋 및 푸시 진행:


```bash
git remote add origin <GitHub-저장소-URL>
git add .
git commit -m "Chore: Initialize repository with README and .gitignore"
git push -u origin main

```




4. **샘플 저장소 검증 (clone 명령어 실습)**

* 별도 경로에서 공개 샘플 저장소 1개를 `git clone`으로 받아 구조 및 로그 확인 후 삭제.





---

## 3단계: Python 콘솔 프로그램 개발 및 커밋 스케줄

외부 라이브러리 없이 표준 문법(리스트, 딕셔너리, 함수 등)으로만 구현하며, 최소 10개 이상의 커밋을 남깁니다.

### [수정 반영된 기본 데이터 구조]



```python
prompts = [
    {
        "title": "수의학 논문 및 임상 데이터 요약",
        "content": "당신은 수의학 및 동물응용과학 전문가입니다. 제시된 반려견 보행 관절 논문의 초록(Abstract)을 읽고 1) 연구 목적, 2) 주요 실험 결과, 3) 한계점 및 시사점을 3줄로 핵심 요약해주세요.",
        "category": "텍스트 생성",
        "favorite": True
    },
    {
        "title": "대형견 전용 착용감 우수한 신발 이미지",
        "content": "A high-quality photographic image of a 32kg Golden Retriever wearing brightly colored, perfectly fitted ergonomic dog shoes, running happily on a grassy park trail, highly detailed, 8k resolution, cinematic lighting.",
        "category": "이미지 생성",
        "favorite": True
    },
    {
        "title": "반려견 헬스케어 및 보행 분석 전문가",
        "content": "당신은 대형견 관절 건강과 기능성 신발을 연구하는 10년 경력의 수의 보행 분석 전문가입니다. 보호자가 질문하는 반려견의 걸음걸이 문제나 발바닥 보호용품에 대해 전문적이면서 친절한 어조로 조언해주세요.",
        "category": "페르소나",
        "favorite": False
    }
]

```

### [기능 개발 및 Git 커밋 스케줄]



| 순서 | 개발 기능 / 단계 | 주요 구현 함수

 | Git 커밋 메시지 예시

 |
| --- | --- | --- | --- |
| **커밋 #1** | 프로젝트 초기화 | - | `Chore: Initialize repository with README and .gitignore` |
| **커밋 #2** | 수정된 기본 데이터 구축 | - | `Feat: Add initial prompt sample data for veterinary and canine research` |
| **커밋 #3** | 메뉴 출력 및 무한루프 | `show_menu()`, `main()` | `Feat: Implement main menu UI and program exit logic` |
| **커밋 #4** | 프롬프트 추가 기능 | `add_prompt()` | `Feat: Add prompt creation functionality with empty check` |
| **브랜치** | **`feature/list` 브랜치 생성 및 전환** (`git checkout -b feature/list`)

 | - | - |
| **커밋 #5** | 전체 목록 보기 기능 | `show_list()` | `Feat: Add prompt listing feature with star indicator` |
| **병합** | **`main` 브랜치로 전환 후 병합** (`git checkout main`, `git merge feature/list`)

 | - | - |
| **커밋 #6** | 카테고리별 조회 기능 | `show_by_category()` | `Feat: Add view prompts by category functionality` |
| **커밋 #7** | 프롬프트 검색 기능 | `search_prompt()` | `Feat: Add keyword search in titles and contents` |
| **커밋 #8** | 프롬프트 상세 보기 | `show_detail()` | `Feat: Add prompt detailed view function` |
| **커밋 #9** | 즐겨찾기 토글/목록 | `toggle_favorite()`, `show_favorites()` | `Feat: Add favorite toggle and favorite list display` |
| **커밋 #10** | README.md 작성 및 보완 | - | `Docs: Complete README.md with project overview and usage` |

---

## 4단계: Git 브랜치 관리 및 원격 반영

1. **브랜치 작업 및 병합 이력 남기기**

* 목록 출력 기능 구현 시 브랜치를 분기하여 작업 후 병합합니다:


```bash
git checkout -b feature/list
# 소스코드 수정 및 commit 진행
git add .
git commit -m "Feat: Add prompt listing feature with star indicator"
git checkout main
git merge feature/list

```




2. **원격 푸시 및 커밋 로그 확인**

* 병합 완료 후 GitHub 원격 저장소에 푸시:


```bash
git push origin main

```


* 터미널에서 로그 및 그래프 확인:


```bash
git log --oneline --graph

```





---

## 5단계: 제출물 점검 및 최종 체크리스트

### 제출물 점검



* **GitHub 저장소 URL** (Public 설정 필수)


* **개발 환경 설정 스크린샷** (VSCode, Python 3.10+ 버전, Git 설정)


* **프로그램 실행 결과 스크린샷** (메뉴, 데이터 추가, 전체 목록, 카테고리별 조회, 검색, 상세 보기, 즐겨찾기)


* **Git 로그 스크린샷** (`git log --oneline --graph` 수행 화면)



### 체크리스트



* [ ] Python 버전이 **3.10 이상**인가?


* [ ] 외부 라이브러리 설치 없이 파이썬 표준 문법만 사용했는가?


* [ ] 수정된 수의학/반려견 관련 기본 프롬프트 데이터 3개가 정상 등록되었는가?


* [ ] 기능별로 함수가 올바르게 분리되어 있는가?


* [ ] 의미 있는 기능 단위 커밋이 **최소 10개 이상** 존재하는가?


* [ ] **`init`, `add`, `commit`, `push`, `pull`, `checkout`, `clone`, `merge**` 명령어를 1회 이상 사용하였는가?


* [ ] 브랜치 생성(`checkout`) 및 병합(`merge`) 그래프 기록이 `git log`에 명확히 보이는가?